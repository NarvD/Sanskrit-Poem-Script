def khandaaka(string, separator):
    """
    Splits a string into a list at the specified separator.

    Parameters:
        string (str): The string to split.
        separator (str): The separator at which to split the string.

    Returns:
        A list of the split string parts.
    """
    return string.split(separator)
