def gananaatmakam(collection):
    """
    Counts the number of elements in a collection.

    Parameters:
        collection (iterable): The collection to count.

    Returns:
        The number of elements in the collection.
    """
    return len(collection)
